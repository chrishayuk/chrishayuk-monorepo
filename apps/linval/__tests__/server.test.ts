//import supertest from "supertest";
//import { createServer } from "../src/server";

describe("server", () => {
  it("root endpoint - linval says hello", async () => {
    // await supertest(createServer())
    //   .get("/")
    //   .set('Accept', 'text/plain')
    //   .expect(200)
    //   .expect("hello, i'm Linval, i'm a very slow but happy image server");
  });
});